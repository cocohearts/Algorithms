This is collection of algorithm classes with corresponding methods written in Python.

Currently, we have:

  -Balanced Binary Search Tree

  -Counting Sort
  
  -Insertion Sort
  
  -Merge Sort
  
  -Quicksort
  
  -Radix Sort
